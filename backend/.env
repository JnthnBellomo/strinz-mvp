# Shasta RPC
TRONGRID_URL=https://api.shasta.trongrid.io

# Your deployed contract
CONTRACT_ADDRESS=TM8piSFrGpD495JRX2u7P8v6s38GS3UXsa

# Allow your frontend origin (dev & Netlify; add others as needed)
ALLOWED_ORIGIN=http://localhost:5173,https://*.netlify.app

# IPFS gateway to fetch audio through (can keep Pinata)
PINATA_GATEWAY=https://gateway.pinata.cloud

# Session token secret (any random string)
SESSION_SECRET=strinz-dev-secret

# Backend port
PORT=8787
eof
